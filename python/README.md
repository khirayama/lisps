# Python Lisp

```
$ python
>> import lisp
>> lisp.eval(lisp.parse("(begin (define r 10) (* pi (* r r)))"))
```

```
$ python
>> import lisp
>> lisp.repl()
lis.py> (begin (define r 10) (* pi (* r r)))
```
